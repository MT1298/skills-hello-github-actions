name: Post welcome comment
on:
  pull_request:
    types: [opened]
permissions:
  pull-requests: write
jobs:

  welcome:
    name: Post welcome comment  
    runs-on: ubuntu-latest
    steps: 
       - name: Post welcome new changes
         run: echo "Welcome to the repository!"

